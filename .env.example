# HTTP runtime settings (used by `npm run dev:http` / `npm run start:http`)
DOCFLOW_HTTP_HOST=127.0.0.1
DOCFLOW_HTTP_PORT=3000

# Optional custom user agent for outbound doc fetches
# DOCFLOW_USER_AGENT=DocFlowMCP/0.1 (+https://github.com/your-username/docflow-mcp)
